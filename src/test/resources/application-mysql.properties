# --- CONFIGURAÇÃO PARA TESTES COM MYSQL REAL ---

spring.config.import=optional:file:.env[.properties]

spring.datasource.url=jdbc:mysql://localhost:3306/testeestagio
spring.datasource.username=apiuser
spring.datasource.password=${mysqldb_user_password}
#spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Importante! O MySQL PRECISA estar rodando
spring.jpa.hibernate.ddl-auto=update

# Opcional: rodar o data.sql
spring.sql.init.mode=always
spring.jpa.show-sql=true